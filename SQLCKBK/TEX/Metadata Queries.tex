\chapter{元数据查询}
\section{列出表的索引列}
对查询来说，知道在哪些列上创建了或没有创建索引很重要。对于选择性极高，因而常用于筛选的列，在它们上面创建索引可以极大地提高查询的性能。在表之间建立连接时，索引也很有用。知道在哪些列上创建了索引后，就在解决性能问题的路上迈出了第一步。另外，你可能还想获悉有关索引本身的信息：有多深（层级数）、有多少个不同的键、有多少个叶子块（leaf block）等。