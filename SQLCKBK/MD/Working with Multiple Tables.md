#

## 从一张表中检索在另一张表中没有对应行的行

关键点：左表连接右表，如果右表不存在，那么右表的字段显示为 `NULL`

## 在查询中添加连接并确保不影响其他连接

## 识别并避免笛卡儿积

通常而言，要避免笛卡儿积，需要应用 $n–1$ 条规则，其中 $n$ 为 FROM 子句中指定的表的个数。因此，要避免笛卡儿积，至少需要 $n–1$ 次连接。根据表的键和用于连接的列，需要的连接可能远远超过 $n–1$ 次，但编写查询时，先考虑使用 $n–1$ 次连接是不错的选择。

## 同时使用连接和聚合
